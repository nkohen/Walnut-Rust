# Fixture provenance

`automaton572.txt` and `automaton2.txt` are copied verbatim from the `walnut-java`
oracle's own golden integration-test corpus
(`src/test/resources/integrationTests/{automaton572,automaton2}.txt`), part of
[Walnut](https://walnut-theorem-prover.github.io/) (GPLv3, Mousavi et al.) — see
`NOTICE` at the repo root. Used here as `.txt` format parser fixtures only; no
walnut-java source code is included.
